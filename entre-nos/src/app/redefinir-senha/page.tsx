'use client'
import Link from'next/link';import{FormEvent,useState}from'react';import{createClient}from'../../lib/supabase'
export default function Page(){const[loading,setLoading]=useState(false),[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);const f=new FormData(e.currentTarget),s=createClient();const{error}=await s.auth.resetPasswordForEmail(String(f.get('email')||'').trim(),{redirectTo:`${location.origin}/redefinir-senha/nova`});setLoading(false);setMsg(error?'Não foi possível enviar o e-mail.':'Se o e-mail estiver cadastrado, enviaremos um link para criar uma nova senha.')}return <main className="center"><div className="panel"><div className="brand">ENTRE <b>NÓS</b></div><h1>Redefinir senha</h1><p>Informe seu e-mail para receber o link de recuperação.</p><form onSubmit={submit}><label>E-mail<input name="email" required type="email"/></label>{msg&&<div className="notice">{msg}</div>}<button className="btn primary" disabled={loading}>{loading?'Enviando...':'Enviar link'}</button></form><p><Link href="/entrar">← Voltar</Link></p></div></main>}