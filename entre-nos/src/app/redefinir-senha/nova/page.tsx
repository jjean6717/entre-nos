'use client'
import{FormEvent,useState}from'react';import{createClient}from'../../../lib/supabase'
export default function Page(){const[loading,setLoading]=useState(false),[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget),a=String(f.get('password')||''),b=String(f.get('confirm')||'');if(a.length<8){setMsg('Use pelo menos 8 caracteres.');return}if(a!==b){setMsg('As senhas não coincidem.');return}setLoading(true);const{error}=await createClient().auth.updateUser({password:a});setLoading(false);if(error){setMsg('Link inválido ou expirado. Solicite outro.');return}setMsg('Senha alterada com sucesso.');setTimeout(()=>location.href='/entrar',1200)}return <main className="center"><div className="panel"><div className="brand">ENTRE <b>NÓS</b></div><h1>Nova senha</h1><form onSubmit={submit}><label>Nova senha<input name="password" required type="password" minLength={8}/></label><label>Confirmar senha<input name="confirm" required type="password" minLength={8}/></label>{msg&&<div className="notice">{msg}</div>}<button className="btn primary" disabled={loading}>{loading?'Salvando...':'Salvar nova senha'}</button></form></div></main>}