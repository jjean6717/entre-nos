'use client'
import Link from'next/link';import{FormEvent,useState}from'react';import{useRouter}from'next/navigation';import{createClient}from'../../lib/supabase'
export default function Page(){const r=useRouter(),[loading,setLoading]=useState(false),[msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setMsg('');setLoading(true);const f=new FormData(e.currentTarget),s=createClient();const{error}=await s.auth.signInWithPassword({email:String(f.get('email')||'').trim(),password:String(f.get('password')||'')});setLoading(false);if(error){setMsg('E-mail ou senha inválidos.');return}r.push('/descobrir');r.refresh()}return <main className="center"><div className="panel"><div className="brand">ENTRE <b>NÓS</b></div><h1>Bem-vindo de volta</h1><p>Entre para continuar suas conversas.</p><form onSubmit={submit}><label>E-mail<input name="email" required type="email"/></label><label>Senha<input name="password" required type="password"/></label>{msg&&<div className="notice">{msg}</div>}<button className="btn primary" disabled={loading}>{loading?'Entrando...':'Entrar'}</button></form><p style={{textAlign:'center'}}><Link href="/redefinir-senha">Esqueci minha senha</Link></p><p>Não tem conta? <Link href="/cadastro">Criar conta</Link></p></div></main>}